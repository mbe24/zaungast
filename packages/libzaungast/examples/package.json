{
  "name": "libzaungast-examples",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Copy-out scaffolding for the libzaungast examples (see README.md). NOT a workspace member and never installed in-repo — in the monorepo the examples resolve via the root install. `npm install` here is only for running the examples OUTSIDE the repo (needs libzaungast >= 0.5, the first release with ./web).",
  "scripts": {
    "build": "node browser-demo/build.mjs",
    "serve": "npx -y serve browser-demo/dist"
  },
  "dependencies": {
    "libzaungast": "^0.5.0"
  },
  "devDependencies": {
    "@sqlite.org/sqlite-wasm": "^3.53.0-build1",
    "esbuild": "^0.28.1"
  }
}
