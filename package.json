{
  "name": "zaungast",
  "version": "0.1.1",
  "description": "Read-only, offline MCP server for Teams — search chats, read conversations, surface trending topics, and find people straight from the local on-disk cache, with no Graph API, no cloud, and no credentials. Not affiliated with or endorsed by Microsoft.",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mbe24/zaungast.git"
  },
  "homepage": "https://github.com/mbe24/zaungast#readme",
  "bugs": "https://github.com/mbe24/zaungast/issues",
  "bin": {
    "zaungast": "dist/index.js"
  },
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && node -e \"require('fs').cpSync('src/schema/versions','dist/schema/versions',{recursive:true})\"",
    "start": "node --experimental-sqlite dist/index.js",
    "dev": "node --experimental-sqlite --import tsx src/index.ts",
    "tsx": "node --experimental-sqlite --import tsx",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-sqlite --import tsx test/unit.ts",
    "test:fixture": "node --experimental-sqlite --import tsx test/fixture/verify.ts",
    "test:integration": "node --experimental-sqlite --import tsx test/_inctest.ts && node --experimental-sqlite --import tsx test/_reusetest.ts && node --experimental-sqlite --import tsx test/_fbtest.ts",
    "assets": "node scripts/render-assets.mjs",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "microsoft-teams",
    "indexeddb",
    "leveldb",
    "offline",
    "read-only"
  ],
  "license": "Apache-2.0",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "@types/node": "^26.1.1",
    "tsx": "^4.23.0",
    "typescript": "^7.0.2"
  }
}
